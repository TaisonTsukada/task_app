<!DOCTYPE html>
<html>
  <head>
    <title>TaskApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark mb-5">
        <div class="container-fluid">
          <%= link_to "タスク管理アプリ",root_path, class: "navbar-brand"%>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <li class="nav-item active">
                <a class="nav-link" aria-current="page" href="#">マイタスク</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">タスク一覧</a>
              </li>
              <li class="nav-item">
                <%= link_to "ユーザー設定", edit_user_registration_path, class: "nav-link", tabindex: "-1"%>
              </li>
              <li class="nav-item ml-5">
                <%if user_signed_in? %>
                  <%= link_to "サインアウト", destroy_user_session_path, method: :delete, class: "nav-link", tabiindex: "-1",data: { confirm: '本当にログアウトしますか?' } %>
                <% else %>
                  <%= link_to "サインイン", new_user_registration_path, tabindex: "-1", class: "nav-link" %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
